# CloudGoat scenario manifest
# This file is consumed by the CloudGoat Python engine.

name: codebuild_buildspec_override
summary: >
  Starting as a low-privileged IAM user with only CodeBuild list, inspect,
  and start permissions, abuse buildspec override injection to execute
  commands under the CodeBuild service role and exfiltrate a Secrets
  Manager secret.
category: aws
difficulty: medium
size: small
